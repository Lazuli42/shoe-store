<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title>Shoes and Shoe Stores</title>
</head>
<body>
    <div class="container">
        <a href="/"><button type="button" class="btn btn-default" aria-label="Left Align">
            <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
        </button></a>
        <h1>{{brand.getName}}</h1>
        <hr>
        <h2>Stores selling this brand:</h2>
        <h6>Is your store missing? Add it to the list.

        <form action="/brand/{{brand.getId}}/added_store" method="post">
            <select name="store_id">
                {% for store in stores %}
                <option value="{{store.getId}}">{{store.getName}}</option>
                {% endfor %}
            </select>

            <button type="submit" class="btn btn-success btn-xs">Submit</button>
        </fom>
        </h6>
        {% for store in brand.getStores %}
            <li>{{store.getName}}</li>
        {% endfor %}
    </div>
</body>
</html>
