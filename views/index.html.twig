<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title>Shoes and Shoe Stores</title>
</head>
<body>
    <div class="container">
        <a href="/"><button type="button" class="btn btn-default" aria-label="Left Align">
            <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
        </button></a>
        <h1>Styilish Shoes and Where to Find Them</h1>
        <hr>
        <div class="row">
            <div class="col-sm-6">
                <h2>Shoes by Store</h2>
                <hr>
                <a href="/create_store"><button class="btn btn-success">Add a Store</button></a>
                <a href="/clear_stores"><button class="btn btn-danger">Delete All Stores</button></a>
                <br><br>

                {% for store in stores %}
                <p><a href="/store/{{store.getId}}">{{store.getName}}</a>

                <form action="/store/{{store.getId}}/delete" method="post">
                  <input name="_method" type="hidden" value="delete">
                  <button type="submit">Delete</button>
                </form></p>
                {% endfor %}

            </div>

            <div class="col-sm-6">
                <h2>Shoes by Brand</h2>
                <hr>
                <a href="/create_brand"><button class="btn btn-success">Add a brand</button></a>
                <a href="/clear_brands"><button class="btn btn-danger">Delete All Brands</button></a>
                <br><br>

                {% for brand in brands %}
                <p><a href="/brand/{{brand.getId}}">{{brand.getName}}</a></p>
                {% endfor %}

            </div>
        </div>
    </div>
</body>
</html>
