<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title>Shoes and Shoe Stores</title>
</head>
<body>
    <div class="container">
        <a href="/"><button type="button" class="btn btn-default" aria-label="Left Align">
            <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
        </button></a>
        <h1>{{store.getName}}
            <button type="button" class="btn btn-default" aria-label="Left Align">
                <a href="/store/{{store.getId}}/edit"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
            </button> </h1>
        <hr>
        <h2>Brands sold here:</h2>
        <h6>Don't see you brand? Add it to the list.

        <form action="/store/{{store.getId}}/added_brand" method="post">
            <select name="brand_id">
                {% for brand in brands %}
                <option value="{{brand.getId}}">{{brand.getName}}</option>
                {% endfor %}
            </select>

            <button type="submit" class="btn btn-success btn-xs">Submit</button>
        </fom>
        </h6>
        {% for brand in store.getBrands %}
            <li>{{brand.getName}}</li>
        {% endfor %}
    </div>
</body>
</html>
